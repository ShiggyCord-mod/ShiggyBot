# Discord bot token (required)
DISCORD_TOKEN=your_token_here

# Command prefix (default: S)
PREFIX=S

# Presence settings
PRESENCE_STATUS=online
PRESENCE_INTERVAL=300

# Optional: log errors to a Discord webhook
LOG_WEBHOOK_URL=

# GitHub repo monitoring (commits, PRs, stars)
GITHUB_REPO=kmmiio99o/ShiggyCord
GITHUB_WEBHOOK_CHANNEL_ID=

# Dashboard (React web app + management API). Leave DASHBOARD_TOKEN empty to disable.
DASHBOARD_TOKEN=
DASHBOARD_PORT=3000
DASHBOARD_WEB_DIR=./web/dist
